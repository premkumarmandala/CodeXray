# CodeXRay Backend

FastAPI backend for C code compilation visualization and execution analysis.

## Setup & Running

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Running Tests

```bash
cd backend
pytest
```
